# Environment setup

Create `.env` in project root:

```bash
VITE_GITHUB_TOKEN=ghp_xxx
VITE_GIST_ID=xxxxxxxxxxxxxxxx
```

`VITE_GITHUB_TOKEN` requires `gist` scope.
`VITE_GIST_ID` is the target gist identifier where `rnos-chat.json` is stored.
